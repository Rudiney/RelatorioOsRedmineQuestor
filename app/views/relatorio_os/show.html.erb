<html>
	<head>
		<title>Relatório de OS - <%= @issue.tracker %> #<%= @issue.id %> - <%= @issue.subject %></title>
		<%= render('estilo') %>
	</head>
	<body>
			<%= render('header') %>
			<div class="issues">
				
				<%= render(:partial => 'issue', :collection => @issue.ancestors, :as => :issue) %>
				
				<%= render(:partial => 'issue', :locals => {:issue => @issue, :children => true}) %>
				
				<%= ("</fieldset>"*(@issue.ancestors.size+1)).html_safe %>
			</div>
			
			<%= render('assinatura') %>
	</body>
</html>