<html>
	<head>
		<title>Relatório de OS - <%= @issue.tracker %> #<%= @issue.id %> - <%= @issue.subject %></title>
		<%= render('estilo') %>
	</head>
	<body>
			<%= render('header') %>
			<div class="issues">
				
				<% @issue.ancestors.each do |issue| %>
					<%= render(:partial => 'issue', :locals => {:issue => issue}) %>
				<% end %>
				
				<%= render(:partial => 'issue', :locals => {:issue => @issue, :children => true}) %>
				
				<%= ("</fieldset>"*(@issue.ancestors.size+8)).html_safe %>
			</div>
			
			<%= render('assinatura') %>
	</body>
</html>